import * as React from 'react';
import { Book } from '../../model/book.model';
import { ReactElement } from 'react';

interface Props {
    books: Book[];
}

export function BookList(props: Props): ReactElement<Props> {
    return (
      <div>
        
      </div>
    );
};
